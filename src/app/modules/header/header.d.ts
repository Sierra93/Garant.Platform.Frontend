export namespace header {
	export interface IItem {
		name: string;
		type: string;
		position: number;
	}

  export interface IHeaderItem {
    name: string;
    icon: string;
    link: string;
  }
}
